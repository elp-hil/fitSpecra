import matplotlib.pyplot as plt
import numpy as np
import dataStruct
import gui
import pdb
window = gui.mainWindow()
nomitSamples = 2000
#currentData = dataStruct.data('/media/group/hyper/archive/Measured_Data/2016_06_21/20160621_081835.mat')
#currentData.read()
#currentData.sum()
#currentData.fft(omitSamples,0)
#currentData.fitParameters[0] = [10, 1, 262100]

#print(currentData.fitParameters)
#fitParameters = currentData.fit(260000, 265000, 0)
#plt.plot(currentData.frequencies, currentData.summedFftData[-currentData.frequencies.size:])
#plt.plot(currentData.frequencies, (currentData.fftData[1, -currentData.frequencies.size:]))
#width = fitParameters[0][0] 
#height = fitParameters[0][1] 
#center = fitParameters[0][2] 
#print('width: ', width, ', center: ', center , ' height: ', height)
#def func(x):
#	# return height * width / ((x - center)**2 + (width/2)**2)
#	return height / (np.pi * (x - center)**2/width + (width))
#yData = func(currentData.frequencies)
#plt.plot(currentData.frequencies, yData)
#plt.show()

